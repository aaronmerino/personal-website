export const metadata = {
  title: 'CONTACT',
}

// `app/page.js` is the UI for the `/` URL
export default function Page() {
  return (<>
            <h1>CONTACT</h1>
            <h2>test</h2>
          </>);
}