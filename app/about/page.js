export const metadata = {
  title: 'ABOUT',
}

// `app/page.js` is the UI for the `/` URL
export default function Page() {
  return (<>
            <h1>ABOUT</h1>
            <h2>test</h2>
          </>);
}